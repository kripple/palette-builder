import Box from '@mui/material/Box';

// Put dynamic styles (the ones that change based on some variable) in the style prop, and put  static styles in the sx prop.

export function App() {
  return <Box data-testid="App">Hello, Bees!</Box>;
}
